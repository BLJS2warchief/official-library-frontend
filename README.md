# Official-library-frontend
### Setup for React
```bash
    sudo apt-get install nodejs
    sudo apt-get install npm
    npm install -g create-react-app
```
### To bootstrap the react application
```bash
    create-react-app library
    cd library
```
